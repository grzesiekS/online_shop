/* SELECTORS */

export const getHomepageData = ({main}) => main.homepage;

/* ACTIONS */

// action name creator
// const reducerName = 'main';
// const createActionName = name => `app/${reducerName}/${name}`;

// Action types

// action creators

//reducer
export default function reducer(statePart = [], action ={}) {
  switch (action.type) {
    default:
      return statePart;
  }
}
